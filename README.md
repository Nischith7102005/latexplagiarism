# LaTeX Plagiarism Checker & Remover

A browser-only prototype for checking and safely rewriting human-readable LaTeX text while preserving commands, math, environments, citations, labels, and references.

Open `index.html` in a browser to use the app.
